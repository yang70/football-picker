<%= form_for @pick, remote: true do |f| %>
  <% if @pick.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pick.errors.count, "error") %> prohibited
                                            this pick from being saved:</h2>

      <ul class="field_with_errors">
      <%= @pick.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.select :winner, ["option 1", "option 2"] %>

  <%= f.submit  %>
<% end %>
