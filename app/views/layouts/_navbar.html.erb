<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Football Picker</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="<%= weeks_path %>" class="dropdown-toggle" data-toggle="dropdown" >Week<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="<%= week_path(get_current_week) %>">Current</a></li>
            <li><a href="<%= week_path(get_current_week - 1) %>">Previous</a></li>
            <li role="separator" class="divider"></li>
            <% (1..17).each do |num| %>
              <li><a href="<%= week_path(num) %>">Week <%= num %></a></li>
            <% end %>
          </ul>
        </li>
        <li><a href="<%= total_scores_path %>">Standings</a></li>
        <% if current_user.present? %>
          <li><%= link_to("Sign Out", destroy_user_session_path, method: :delete) %></li>
          <li><a>Signed in as <%= current_user.email %></a></li>
        <% else %>
          <li><a href="<%= new_user_registration_path %>">Sign Up</a></li>
          <li><a href="<%= new_user_session_path %>">Sign In</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
